The program, threading, often seg faults when it runs. Analyze the code in threading.c
and fix it so that it does not seg fault.

To build your code run 'make' in the terminal below.
To run your code run './threading' in the terminal below.